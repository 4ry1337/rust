## Functions

*Statements* are instructions that perform some action and do not return a value.

*Expressions* evaluate to a resultant value.

```rust
fn function(x: i32) { // pass by value
    println!("The value of x is: {x}");
}

fn function(x: i32) {
    println!("The value of x is: {x}");
}
```
